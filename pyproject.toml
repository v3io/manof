[project]
name = "manof"
requires-python = ">=3.9"
version = "0.1.3"
authors = [
    {name = "Iguazio"}
]

[tool.setuptools]
py-modules = []

# please keep this to a minimum - defaults are good
[tool.black]
exclude = '''
^/(   # anchor to the root
  (   # exclude these directories
    \.git
    | \.venv
    | venv
  )/
)
'''
